#include <stdlib.h>
#include <stdio.h>

/**
* main - Prints the digits of the decimal number system
*
* Return: 0 when code executes succesfully
*/

int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);
	printf("\n");
	return (0);
}
